import React from 'react';
import ReactDOM from 'react-dom/client';
import { WikiFootballApp } from './WikiFootballApp';

import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <WikiFootballApp />
    </React.StrictMode>
);
